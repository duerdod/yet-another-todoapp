import styled from '@emotion/styled';

const Button = styled.button`
  cursor: pointer;
  margin-left: 22px;
  font-size: 2rem;
  background: none;
  border: 0;
  outline: 0;
`;

export default Button;

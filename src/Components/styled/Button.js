import styled from '@emotion/styled';

const Button = styled.button`
  cursor: pointer;
  margin-left: 18px;
  font-size: 1.2rem;
  background: none;
  border: 0;
  outline: 0;
`;

export default Button;
